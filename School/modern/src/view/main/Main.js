/* Ext.define('School.view.main.Main', {
    extend: 'Ext.tab.Panel',
    xtype: 'app-main',

    requires: [
        'Ext.MessageBox',
        'Ext.layout.Fit'
    ],

    controller: 'main',
    viewModel: 'main',

     defaults: {
        tab: {
            iconAlign: 'top'
        }
    }, 

    //tabBarPosition: 'bottom',

    items: [
        {
            items: [{
                xtype: 'mainlist'
            }]
        }
    ]
});
 */